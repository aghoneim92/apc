import * as React from 'react'
import { Patient } from 'src/NewPatientForm/Patient'

interface Props {
  patients: Patient[]
}

const AllPatients = (props: Props) => <div />

export default AllPatients
